package Puzzle_Lab.Enums;

/**
 * Used to represent the direction a hint is pointing to. For example, there
 * could be two words, each starting from the same cell and thus having the same
 * label. But one hint would be for the word going DOWN, and the other ACROSS.
 */
public enum Direction {
    ACROSS, DOWN
}
